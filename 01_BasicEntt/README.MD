# 01_BasicEntt
このプロジェクトは、**EnTT の最小サンプル**として、``ECS``の基礎となる概念を学ぶための章です。

## 📘 学べること
- `registry.create()` による``Entity``の生成  
- `registry.emplace<T>()` による``Component``の追加  
- `registry.view<T>()` による``Component``の取得と操作
- `view.each()` を使った``Entity``の列挙
- ``EnTT``の``ECS``ワークフローの最小構成